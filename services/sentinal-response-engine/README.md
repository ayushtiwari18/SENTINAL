# ⚠️ This service has been renamed

This directory (`services/sentinal-response-engine`) has been renamed to `services/nexus-agent`.

All code, config, and policy files are now at:
```
services/nexus-agent/
```

This directory is kept only as a redirect marker and will be removed in a future cleanup.
